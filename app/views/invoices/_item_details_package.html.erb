<% product['subscriptions'].each do |item| %>
  <%= render 'invoices/shared/item_list', title: item['name'],
        sub_title: t("product_types.#{ item['type'] }"), amount: amount_to_currency(item['cost']) %>
<% end %>
